1. Module is a javscript file which contains functions
    - we export a fuction by syntax
    - module.exports = {functionName}
    - to import module we require method and pass our module file


2. A package is a group of module 
    - for example react is a package
    - useState, useEffect are module of react package 